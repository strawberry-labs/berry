import*as React from"react";import{Download,Plus,Save,Search}from"lucide-react";import{AsyncState,Button,Checkbox,DataTable,DualTrend,FormSelect,Input,ManagementPage,MetricGrid,MiniSeries,PermissionDenied,SearchInput,Section,StatusPill,SuccessMessage,Toolbar,formatDateTime,formatMoney,formatNumber}from"./management-primitives";import{useResource,type ManagementScreenProps}from"./management-context";import{OrganizationProfileScreen}from"./organization-profile-screen";import{ReportsAlertsScreen}from"./reports-alerts-screen";import{AnalyticsScreen}from"./analytics-screen";import{AdminCatalogScreen}from"./admin-catalog-screens";
export function AdminScreen({tab,...p}:ManagementScreenProps&{tab:string}){const read=permissionFor(tab);if(read&&!p.permissions.includes(read as any))return <PermissionDenied label={titleFor(tab)}/>;if(tab==="overview")return <Overview {...p}/>;if(tab==="members")return <Members {...p}/>;if(tab==="departments")return <Departments {...p}/>;if(tab==="analytics")return <AnalyticsScreen {...p}/>;if(tab==="spend-limits")return <SpendLimits {...p}/>;if(tab==="credits-billing")return <Billing {...p}/>;if(tab==="reports-alerts")return <ReportsAlertsScreen {...p}/>;if(tab==="execution-network")return <PolicyScreen kind="execution" {...p}/>;if(tab==="authentication")return <PolicyScreen kind="authentication" {...p}/>;if(tab==="data-governance")return <PolicyScreen kind="data" {...p}/>;if(tab==="service-accounts")return <ServiceAccounts {...p}/>;if(tab==="profile-domains")return <OrganizationProfileScreen {...p}/>;return <AdminCatalogScreen tab={tab}{...p}/>}
function Overview({client,config,tenantId}:ManagementScreenProps){const now=new Date(),to=now.toISOString(),from=new Date(now.getTime()-30*864e5).toISOString();const r=useResource(`overview:${tenantId}`,async()=>client?Promise.all([client.listOrgMembers(tenantId),client.usageAnalytics(tenantId,{from,to,limit:20}),client.billingHealth(tenantId),client.listAuditEvents(tenantId,{limit:8})]):[[],config.usageDashboards.find(x=>x?.tenantId===tenantId)??null,null,config.auditEvents]as any,[]as any);const[members,usage,health,audit]=r.data;return <ManagementPage title="Overview"description="Operational health, adoption, spend, and recent administration activity."eyebrow="Organization administration"><AsyncState loading={r.loading}error={r.error}onRetry={r.retry}>{usage?<><MetricGrid items={[{label:"Active members",value:formatNumber(members.length?members.filter((m:any)=>m.status==="active").length:(usage.byUser??[]).filter((u:any)=>u.userId).length)},{label:"Billed spend",value:formatMoney(usage.totals?.billedCostMicros??usage.totals?.costBilledMicros)},{label:"Requests",value:formatNumber(usage.totals?.requests??0)},{label:"Successful requests",value:usage.totals?.successRate==null?"—":`${Math.round(usage.totals.successRate*100)}%`,status:(usage.totals?.successRate??1)<.9?"warning":"good"}]}/><div className="mgmt-two-column"><Section title="Spend and request trend"><DualTrend label="Billed spend"points={(usage.series??usage.burnDown??[]).map((x:any)=>({label:(x.ts??x.date).slice(0,10),spend:Number(x.billedCostMicros??x.costBilledMicros)/1e6,requests:Number(x.requests??0)}))}spendFormat={v=>formatMoney(v*1e6)}/></Section><Section title="System health"><div className="mgmt-health-list"><p><StatusPill tone={health?.status==="healthy"?"good":"warning"}>{health?.status??"Demo"}</StatusPill><span>Billing and reservations</span></p><p><StatusPill tone="good">Healthy</StatusPill><span>Usage ingestion</span></p><p><StatusPill tone="good">Healthy</StatusPill><span>Audit chain</span></p></div></Section></div><Section title="Recent admin activity"><DataTable label="Recent activity"columns={["Action","Actor","When"]}rows={audit.map((x:any)=>[<b>{human(x.action)}</b>,x.actorUserId??"System",formatDateTime(x.ts??x.createdAt)])}/></Section></>:null}</AsyncState></ManagementPage>}
function Members({client,config,tenantId,permissions}:ManagementScreenProps){const[query,setQuery]=React.useState(""),[open,setOpen]=React.useState(false),[message,setMessage]=React.useState("");const r=useResource(`members:${tenantId}`,async()=>client?client.listOrgMembers(tenantId):[],[]as any[]);const rows=r.data.filter((m:any)=>`${m.name} ${m.email} ${m.role}`.toLowerCase().includes(query.toLowerCase()));const submit=async(e:React.FormEvent<HTMLFormElement>)=>{e.preventDefault();if(!client)return;const f=new FormData(e.currentTarget);await client.createOrgMember(tenantId,{email:String(f.get("email")),name:String(f.get("name")),password:String(f.get("password")),role:String(f.get("role"))as any});setOpen(false);setMessage("Member invited and the default allowance profile was applied.");r.retry()};return <ManagementPage title="Members"description="Search, invite, and administer organization members. SCIM-managed identities remain locked."eyebrow="People"actions={permissions.includes("members:write")?<Button onClick={()=>setOpen(true)}><Plus/>Invite member</Button>:null}><Toolbar><SearchInput label="Search members"value={query}onChange={setQuery}placeholder="Search name, email, or role"/></Toolbar>{message?<SuccessMessage>{message}</SuccessMessage>:null}{open?<form className="mgmt-inline-form"onSubmit={submit}><label>Name<Input name="name"required/></label><label>Email<Input name="email"type="email"required/></label><label>Temporary password<Input name="password"type="password"minLength={12}required/></label><label>Role<FormSelect name="role" defaultValue="member" options={[{ value: "member", label: "Member" }, { value: "admin", label: "Admin" }, { value: "owner", label: "Owner" }]} /></label><Button type="button" variant="secondary" onClick={()=>setOpen(false)}>Cancel</Button><Button>Send invitation</Button></form>:null}<AsyncState loading={r.loading}error={r.error}onRetry={r.retry}empty={rows.length===0}><DataTable label="Members"columns={["Member","Role","Departments","Source","Status"]}rows={rows.map((m:any)=>[<span><b>{m.name}</b><small>{m.email}</small></span>,m.role,(m.departmentIds??[]).join(", ")||"—",m.source,<StatusPill tone={m.status==="active"?"good":"warning"}>{m.status}</StatusPill>])}/></AsyncState></ManagementPage>}
function Departments({client,config,tenantId,permissions}:ManagementScreenProps){const[open,setOpen]=React.useState(false);const r=useResource(`departments:${tenantId}`,async()=>client?client.listDepartments(tenantId):config.departments.filter(d=>d.tenantId===tenantId),[]as any[]);const submit=async(e:React.FormEvent<HTMLFormElement>)=>{e.preventDefault();const f=new FormData(e.currentTarget);const parentId=String(f.get("parentId"));await client?.createDepartment(tenantId,{name:String(f.get("name")),slug:String(f.get("slug")),parentId:parentId==="none"?null:parentId});setOpen(false);r.retry()};return <ManagementPage title="Departments"description="Nested ownership, membership, inherited policies, and departmental spend controls."eyebrow="People"actions={permissions.includes("departments:write")?<Button onClick={()=>setOpen(true)}><Plus/>New department</Button>:null}>{open?<form className="mgmt-inline-form"onSubmit={submit}><label>Name<Input name="name"required/></label><label>Slug<Input name="slug"pattern="[a-z0-9-]+"required/></label><label>Parent<FormSelect name="parentId" defaultValue="none" options={[{ value: "none", label: "Top level" }, ...r.data.map((d: any) => ({ value: d.id, label: d.name }))]} /></label><Button>Create</Button></form>:null}<AsyncState loading={r.loading}error={r.error}onRetry={r.retry}empty={r.data.length===0}><DataTable label="Departments"columns={["Department","Parent","Status","Updated"]}rows={r.data.map((d:any)=>[<b>{d.name}</b>,r.data.find((x:any)=>x.id===d.parentId)?.name??"Organization",<StatusPill tone={d.status==="active"?"good":"neutral"}>{d.status}</StatusPill>,new Date(d.updatedAt).toLocaleDateString()])}/></AsyncState></ManagementPage>}
function Analytics({client,tenantId}:ManagementScreenProps){const initial=React.useMemo(()=>{const q=new URLSearchParams(location.search),to=q.get("to")??new Date().toISOString().slice(0,10),from=q.get("from")??new Date(Date.now()-30*864e5).toISOString().slice(0,10);return{from,to}},[]);const[range,setRange]=React.useState(initial);const from=`${range.from}T00:00:00.000Z`,to=`${range.to}T23:59:59.999Z`;const r=useResource(`analytics:${tenantId}:${from}:${to}`,async()=>client?client.usageAnalytics(tenantId,{from,to,limit:50}):null,null as any);const apply=(next:any)=>{setRange(next);history.replaceState(null,"",`${location.pathname}?from=${next.from}&to=${next.to}`)};return <ManagementPage title="Analytics"description="Filterable billed usage, performance, attribution, request detail, and explainable anomalies."eyebrow="Finance"actions={<Button variant="secondary"onClick={()=>client?.exportUsageCsv(tenantId,{from,to}).then(csv=>download(csv,"organization-usage.csv"))}><Download/>Export CSV</Button>}><Toolbar><label>From<Input type="date"value={range.from}onChange={e=>apply({...range,from:e.currentTarget.value})}/></label><label>To<Input type="date"value={range.to}onChange={e=>apply({...range,to:e.currentTarget.value})}/></label></Toolbar><AsyncState loading={r.loading}error={r.error}onRetry={r.retry}empty={!r.data||r.data.totals.requests===0}>{r.data?<><MetricGrid items={[{label:"Billed spend",value:formatMoney(r.data.totals.billedCostMicros)},{label:"Requests",value:formatNumber(r.data.totals.requests)},{label:"Tokens",value:formatNumber(r.data.totals.tokens)},{label:"Projected month-end",value:formatMoney(r.data.totals.projectedMonthEndMicros)}]}/><Section title="Spend trend"><MiniSeries label="Daily spend"points={r.data.series.map((p:any)=>({label:p.ts.slice(0,10),value:Number(p.billedCostMicros)/1e6}))}format={v=>formatMoney(v*1e6)}/></Section>{r.data.anomalies.length?<Section title="Needs attention">{r.data.anomalies.map((a:any)=><article className="mgmt-anomaly"key={a.id}><StatusPill tone={a.severity==="error"?"danger":"warning"}>{a.severity}</StatusPill><div><b>{a.label}</b><p>{a.explanation}</p></div></article>)}</Section>:null}<Section title="Models"><DataTable label="Model analytics"columns={["Model","Requests","Tokens","Billed","Errors","P95 latency"]}rows={(r.data.breakdowns.models??[]).map((x:any)=>[x.label,formatNumber(x.requests),formatNumber(x.tokens),formatMoney(x.billedCostMicros),x.errorRate==null?"—":`${Math.round(x.errorRate*100)}%`,x.latencyP95Ms==null?"—":`${x.latencyP95Ms} ms`])}/></Section></>:null}</AsyncState></ManagementPage>}
function SpendLimits({client,tenantId,permissions}:ManagementScreenProps){const r=useResource(`limits:${tenantId}`,async()=>client?Promise.all([client.listBudgetLimits(tenantId),client.listAllowanceProfiles(tenantId)]):[[],[]],[]as any);const[success,setSuccess]=React.useState("");const submit=async(e:React.FormEvent<HTMLFormElement>)=>{e.preventDefault();const f=new FormData(e.currentTarget);await client?.bulkUpsertAllowanceLimits(tenantId,{idempotencyKey:crypto.randomUUID(),reason:String(f.get("reason")),dryRun:false,items:[{scopeType:String(f.get("scopeType"))as any,scopeId:String(f.get("scopeId")),period:String(f.get("period"))as any,softLimitMicros:String(Math.round(Number(f.get("soft"))*1e6)),hardLimitMicros:String(Math.round(Number(f.get("hard"))*1e6)),requestLimit:Number(f.get("requests"))||null,tokenLimit:Number(f.get("tokens"))||null,sandboxMinuteLimit:Number(f.get("sandbox"))||null}]});setSuccess("Allowance applied and recorded in the audit log.");r.retry()};return <ManagementPage title="Spend limits"description="Organization credits stay pooled. Members and departments receive restrictive allowances—not wallets."eyebrow="Finance"><Section title="Effective hierarchy"description="The most restrictive applicable organization, department, or user rule wins."><DataTable label="Spend limits"columns={["Scope","Period","Soft","Hard","Requests","Tokens","Sandbox minutes","Updated"]}rows={(r.data[0]??[]).map((x:any)=>[`${x.scopeType}: ${x.scopeId}`,x.period,formatMoney(x.softLimitMicros),formatMoney(x.hardLimitMicros),x.requestLimit??"—",x.tokenLimit??"—",x.sandboxMinuteLimit??"—",new Date(x.updatedAt).toLocaleDateString()])}/></Section>{permissions.includes("budgets:write")?<Section title="Assign allowance"description="Bulk APIs apply this atomically and idempotently; reservations count against availability."><form className="mgmt-inline-form"onSubmit={submit}><label>Scope<FormSelect name="scopeType" defaultValue="user" options={[{ value: "user", label: "User" }, { value: "department", label: "Department" }, { value: "org", label: "Organization" }]} /></label><label>Scope ID<Input name="scopeId"required/></label><label>Period<FormSelect name="period" defaultValue="month" options={[{ value: "month", label: "Monthly" }, { value: "day", label: "Daily" }]} /></label><label>Soft limit (USD)<Input name="soft"type="number"min="0"step=".01"required/></label><label>Hard limit (USD)<Input name="hard"type="number"min="0"step=".01"required/></label><label>Request quota<Input name="requests"type="number"min="0"/></label><label>Token quota<Input name="tokens"type="number"min="0"/></label><label>Sandbox minutes<Input name="sandbox"type="number"min="0"step=".1"/></label><label className="mgmt-field-wide">Audit reason<Input name="reason"minLength={3}required/></label><Button><Save/>Apply allowance</Button></form>{success?<SuccessMessage>{success}</SuccessMessage>:null}</Section>:null}</ManagementPage>}
function Billing({client,tenantId,permissions}:ManagementScreenProps){const r=useResource(`billing:${tenantId}`,async()=>client?Promise.all([client.billingSummary(tenantId),client.billingHealth(tenantId),client.billingLedger(tenantId),client.autoRefill(tenantId)]):[null,null,{items:[]},null],[]as any);const[confirmed,setConfirmed]=React.useState(false),[success,setSuccess]=React.useState("");const submit=async(e:React.FormEvent<HTMLFormElement>)=>{e.preventDefault();const f=new FormData(e.currentTarget);await client?.createBillingCreditGrant(tenantId,{source:"manual",amountMicros:String(Math.round(Number(f.get("amount"))*1e6)),currency:"usd",reason:String(f.get("reason")),externalRef:String(f.get("externalRef"))||null,confirmation:true,idempotencyKey:String(f.get("idempotencyKey")),metadata:{}});setConfirmed(false);setSuccess("Organization credit grant completed.");r.retry()};const[s,h,l,a]=r.data;return <ManagementPage title="Credits & billing"description="The organization owns one prepaid pool. Reservations, reconciliations, grants, and invoices are auditable."eyebrow="Finance"><AsyncState loading={r.loading}error={r.error}onRetry={r.retry}>{s?<><MetricGrid items={[{label:"Prepaid balance",value:formatMoney(s.prepaidBalanceMicros)},{label:"Provider",value:s.provider},{label:"Billing health",value:h?.status??"Unknown",status:h?.status==="healthy"?"good":"warning"},{label:"Auto-refill",value:a?.supported?(a.enabled?"On":"Off"):"Unsupported"}]}/><Section title="Credit ledger"><DataTable label="Credit ledger"columns={["Time","Kind","Amount","Balance","Source","Status"]}rows={(l.items??[]).map((x:any)=>[new Date(x.createdAt).toLocaleString(),x.kind,formatMoney(x.amountMicros),formatMoney(x.balanceAfterMicros),x.source,<StatusPill tone="good">{x.status}</StatusPill>])}/></Section>{permissions.includes("billing:write")?<Section title="Manual credit grant"description="Confirmation and a unique idempotency key are required. This changes the organization pool."><form className="mgmt-inline-form"onSubmit={submit}><label>Amount (USD)<Input name="amount"type="number"min=".01"step=".01"required/></label><label>External reference<Input name="externalRef"/></label><label>Idempotency key<Input name="idempotencyKey"defaultValue={crypto.randomUUID()}minLength={8}required/></label><label className="mgmt-field-wide">Reason<Input name="reason"minLength={3}required/></label><label className="mgmt-confirm mgmt-field-wide"><Checkbox checked={confirmed} onCheckedChange={(checked) => setConfirmed(checked === true)} /><span>I confirm this financial mutation changes the organization credit pool.</span></label><Button variant="destructive"disabled={!confirmed}>Create credit grant</Button></form>{success?<SuccessMessage>{success}</SuccessMessage>:null}</Section>:null}</>:null}</AsyncState></ManagementPage>}
function ReportsAlerts({client,tenantId,permissions}:ManagementScreenProps){const r=useResource(`reports:${tenantId}`,async()=>client?Promise.all([client.savedAnalyticsViews(tenantId),client.reportSchedules(tenantId),client.reportRuns(tenantId),client.alertDestinations(tenantId),client.alertRules(tenantId),client.alertDeliveries(tenantId)]):[[],[],[],[],[],[]],[]as any);const[v,s,runs,d,rules,deliveries]=r.data;return <ManagementPage title="Reports & alerts"description="Saved analytic views, scheduled delivery, alert rules, destinations, and delivery history."eyebrow="Finance"><AsyncState loading={r.loading}error={r.error}onRetry={r.retry}><div className="mgmt-two-column"><Section title="Scheduled reports"><DataTable label="Report schedules"columns={["Name","Cadence","Format","Status","Next run"]}rows={s.map((x:any)=>[x.name,x.cadence,x.format,<StatusPill tone={x.status==="active"?"good":"neutral"}>{x.status}</StatusPill>,x.nextRunAt?new Date(x.nextRunAt).toLocaleString():"—"])}/></Section><Section title="Alert rules"><DataTable label="Alert rules"columns={["Rule","Signal","Threshold","State"]}rows={rules.map((x:any)=>[x.name,human(x.signal),x.threshold,x.enabled?<StatusPill tone="good">On</StatusPill>:"Off"])}/></Section></div><Section title="Delivery history"><DataTable label="Delivery history"columns={["Created","Destination","Attempt","Status","Error"]}rows={deliveries.map((x:any)=>[new Date(x.createdAt).toLocaleString(),x.destinationId,x.attempt,<StatusPill tone={x.status==="delivered"?"good":x.status==="failed"?"danger":"warning"}>{x.status}</StatusPill>,x.error??"—"])}/></Section>{permissions.includes("reports:write")&&v.length===0?<p className="mgmt-callout">Save an analytics view from Analytics before creating a report schedule.</p>:null}{d.length===0?<p className="mgmt-callout">No alert destination is configured. Add one through the API or deployment configuration before enabling delivery.</p>:null}</AsyncState></ManagementPage>}
function PolicyScreen({kind,client,tenantId,permissions}:ManagementScreenProps&{kind:"execution"|"authentication"|"data"}){const write=kind==="execution"?"guardrails:write":kind==="authentication"?"auth_policy:write":"data_policy:write";const r=useResource(`${kind}:${tenantId}`,async()=>{if(!client)return null;return kind==="execution"?client.executionPolicy(tenantId):kind==="authentication"?client.authenticationPolicy(tenantId):client.dataGovernancePolicy(tenantId)},null as any);const[message,setMessage]=React.useState("");const save=async()=>{if(!client||!r.data)return;if(kind==="execution")await client.updateExecutionPolicy(tenantId,strip(r.data));else if(kind==="authentication")await client.updateAuthenticationPolicy(tenantId,strip(r.data));else await client.updateDataGovernancePolicy(tenantId,strip(r.data));setMessage("Policy saved and added to the organization audit log.");r.retry()};return <ManagementPage title={kind==="execution"?"Execution & network":kind==="authentication"?"Authentication":"Data governance"}description={kind==="execution"?"Sandbox, approvals, network access, concurrency, rate, token, and sandbox-minute controls.":kind==="authentication"?"MFA, session lifetime, trusted devices, login methods, domains, and emergency owner access.":"Retention, residency, filters, moderation hooks, deletion, export, and legal-hold behavior."}eyebrow="Security & data"><AsyncState loading={r.loading}error={r.error}onRetry={r.retry}>{r.data?<Section title="Effective policy"actions={permissions.includes(write as any)?<Button onClick={save}><Save/>Save policy</Button>:null}><div className="mgmt-policy-grid">{Object.entries(r.data).filter(([k])=>!['tenantId','updatedAt'].includes(k)).map(([k,v])=><label key={k}><span>{human(k)}</span>{typeof v==="boolean"?<Checkbox checked={v} disabled={!permissions.includes(write as any)} onCheckedChange={(checked) => r.setData({ ...r.data, [k]: checked === true })} />:typeof v==="number"?<Input type="number"value={v}disabled={!permissions.includes(write as any)}onChange={e=>r.setData({...r.data,[k]:Number(e.currentTarget.value)})}/>:Array.isArray(v)?<Input value={v.join(", ")}disabled={!permissions.includes(write as any)}onChange={e=>r.setData({...r.data,[k]:e.currentTarget.value.split(",").map(x=>x.trim()).filter(Boolean)})}/>:typeof v==="object"?<code>{JSON.stringify(v)}</code>:<Input value={String(v??"")}disabled={!permissions.includes(write as any)}onChange={e=>r.setData({...r.data,[k]:e.currentTarget.value})}/>}</label>)}</div>{message?<SuccessMessage>{message}</SuccessMessage>:null}</Section>:null}</AsyncState></ManagementPage>}
function ServiceAccounts({client,tenantId,permissions}:ManagementScreenProps){const r=useResource(`accounts:${tenantId}`,async()=>client?client.serviceAccounts(tenantId):[],[]as any[]);const[token,setToken]=React.useState<string|null>(null);const create=async(e:React.FormEvent<HTMLFormElement>)=>{e.preventDefault();const f=new FormData(e.currentTarget);const row=await client?.createServiceAccount(tenantId,{name:String(f.get("name")),permissions:["org:read"],resourceRestrictions:[],expiresAt:null});if(row)setToken(row.token);r.retry()};return <ManagementPage title="Service accounts"description="Scoped non-human principals with expiry, rotation, revocation, and one-time tokens."eyebrow="Security & data">{token?<div className="mgmt-secret"role="status"><b>Copy this token now</b><code>{token}</code><span>Berry stores only a hash. This token cannot be shown again.</span></div>:null}{permissions.includes("service_accounts:write")?<form className="mgmt-inline-form"onSubmit={create}><label>Account name<Input name="name"required/></label><Button><Plus/>Create account</Button></form>:null}<AsyncState loading={r.loading}error={r.error}onRetry={r.retry}empty={r.data.length===0}><DataTable label="Service accounts"columns={["Name","Permissions","Status","Token","Last used","Expires"]}rows={r.data.map((x:any)=>[<b>{x.name}</b>,x.permissions.join(", "),<StatusPill tone={x.status==="active"?"good":"danger"}>{x.status}</StatusPill>,`•••• ${x.tokenLast4}`,x.lastUsedAt?new Date(x.lastUsedAt).toLocaleString():"Never",x.expiresAt?new Date(x.expiresAt).toLocaleDateString():"Never"])}/></AsyncState></ManagementPage>}
function Profile({client,tenantId}:ManagementScreenProps){const r=useResource(`profile:${tenantId}`,async()=>client?client.organizationProfile(tenantId):null,null as any);return <ManagementPage title="Profile & domains"description="Organization identity, locale, contacts, verified domains, branding, and deployment metadata."eyebrow="Organization"><AsyncState loading={r.loading}error={r.error}onRetry={r.retry}>{r.data?<><MetricGrid items={[{label:"Organization",value:r.data.name},{label:"Slug",value:r.data.slug},{label:"Deployment",value:r.data.deploymentMode},{label:"Region",value:r.data.region??"Default"}]}/><Section title="Verified domains"><DataTable label="Organization domains"columns={["Domain","Status","Custom domain","Verified"]}rows={r.data.domains.map((x:any)=>[x.domain,<StatusPill tone={x.status==="verified"?"good":"warning"}>{x.status}</StatusPill>,x.customDomain?"Yes":"No",x.verifiedAt?new Date(x.verifiedAt).toLocaleDateString():"—"])}/></Section><Section title="Contacts"><DataTable label="Contacts"columns={["Purpose","Address"]}rows={[["Support",r.data.supportEmail??"Not configured"],["Security",r.data.securityEmail??"Not configured"]]}/></Section></>:null}</AsyncState></ManagementPage>}
function permissionFor(tab:string){return({members:"members:read",departments:"departments:read",roles:"rbac:read","resource-access":"acl:read",models:"models:read","feature-access":"feature_flags:read","execution-network":"guardrails:read",analytics:"usage:read","spend-limits":"budgets:read","credits-billing":"billing:read","reports-alerts":"reports:read","sso-scim":"sso:read","managed-policy":"policy:read",authentication:"auth_policy:read","data-governance":"data_policy:read","service-accounts":"service_accounts:read","audit-log":"audit:read","profile-domains":"org_settings:read"}as Record<string,string>)[tab]}
function titleFor(tab:string){return({roles:"Roles & permissions","resource-access":"Resource access",models:"Models","skills-mcp":"Skills & MCP","feature-access":"Feature access","sso-scim":"SSO & SCIM","managed-policy":"Managed policy","audit-log":"Audit log"}as Record<string,string>)[tab]??human(tab)}
function human(value:string){return value.replaceAll("_"," ").replaceAll("-"," ").replace(/\b\w/g,c=>c.toUpperCase())}function strip(value:any){const{tenantId,updatedAt,...rest}=value;return rest}function download(value:string,name:string){const a=document.createElement("a");a.href=URL.createObjectURL(new Blob([value],{type:"text/csv"}));a.download=name;a.click();URL.revokeObjectURL(a.href)}
