import type{ManagementScreenProps}from"./management-context";import{AdminScreen}from"./admin-screens";import{PersonalSettingsScreen}from"./personal-settings-screens";import{PlatformScreen}from"./platform-screens";import type{ManagementKind}from"./management-navigation";
export function ManagementExperience({kind,tab,...props}:ManagementScreenProps&{kind:ManagementKind;tab:string}){return <section className="mgmt-experience"aria-label={kind==="settings"?"Personal settings":kind==="admin"?"Organization administration":"Platform operations"}>{props.config.demoMode?<div className="mgmt-demo-banner"role="status">Demo adapter — data on this screen is local and cannot mutate a deployment.</div>:null}{kind==="settings"?<PersonalSettingsScreen tab={tab}{...props}/>:kind==="admin"?<AdminScreen tab={tab}{...props}/>:<PlatformScreen tab={tab}{...props}/>}</section>}
