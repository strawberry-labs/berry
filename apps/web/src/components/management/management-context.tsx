import*as React from"react";import type{BerryApiClient}from"@berry/api-client";import type{OrgPermission,Task,Workspace}from"@berry/shared";import type{WebConfig}from"@/lib/config";
export type ManagementScreenProps={client:BerryApiClient|null;config:WebConfig;tenantId:string;userId:string|null;permissions:OrgPermission[];onUsePrompt:(prompt:string)=>void;tasks:Task[];workspaces:Workspace[];onArchiveTask:(task:Task,archived:boolean)=>Promise<void>;onDeleteTask:(task:Task)=>Promise<void>;onRestoreTask:(task:Task)=>Promise<void>};
export function useResource<T>(key:string,loader:()=>Promise<T>,fallback:T){const[data,setData]=React.useState<T>(fallback),[loading,setLoading]=React.useState(true),[error,setError]=React.useState<string|null>(null),[version,setVersion]=React.useState(0);React.useEffect(()=>{let active=true;setLoading(true);setError(null);void loader().then(value=>{if(active)setData(value)}).catch(cause=>{if(active)setError(cause instanceof Error?cause.message:"Unable to load data")}).finally(()=>{if(active)setLoading(false)});return()=>{active=false}},[key,version]);return{data,loading,error,retry:()=>setVersion(v=>v+1),setData};}
export function useLocalSetting(key:string,fallback:string){const[value,setValue]=React.useState(fallback);React.useEffect(()=>setValue(localStorage.getItem(key)??fallback),[key,fallback]);return[value,(next:string)=>{setValue(next);localStorage.setItem(key,next)}]as const;}
