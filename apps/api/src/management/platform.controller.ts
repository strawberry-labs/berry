import { Body, Controller, ForbiddenException, Get, Inject, Param, Post, Req } from "@nestjs/common";
import { BillingOperationsSummarySchema, PlatformOverviewSchema, PlatformRolloutMutationSchema, PlatformTenantLifecycleMutationSchema, PlatformTenantProvisionSchema, PlatformTenantSummarySchema, RolloutRuleSchema, RouterHealthSummarySchema } from "@berry/shared";
import { z } from "zod";import type{AuthenticatedRequest}from"../auth/auth.guard.ts";import{PLATFORM_AUTHORIZER,type PlatformAuthorizer}from"./platform-authorizer.ts";import{PLATFORM_SERVICE,type PlatformService}from"./platform.service.ts";
@Controller("/v1/platform") export class PlatformController{constructor(@Inject(PLATFORM_AUTHORIZER)private a:PlatformAuthorizer,@Inject(PLATFORM_SERVICE)private s:PlatformService){}private async ok(r:AuthenticatedRequest){if(!await this.a.authorize(r))throw new ForbiddenException("Platform operator authorization required");}
@Get("/overview")async overview(@Req()r:AuthenticatedRequest){await this.ok(r);return PlatformOverviewSchema.parse(await this.s.overview());}@Get("/organizations")async tenants(@Req()r:AuthenticatedRequest){await this.ok(r);return z.array(PlatformTenantSummarySchema).parse(await this.s.tenants());}@Post("/organizations")async provision(@Req()r:AuthenticatedRequest,@Body()b:unknown){await this.ok(r);return PlatformTenantSummarySchema.parse(await this.s.provisionTenant(PlatformTenantProvisionSchema.parse(b),r.auth!.user.id));}@Post("/organizations/:tenantId/lifecycle")async lifecycle(@Req()r:AuthenticatedRequest,@Param("tenantId")t:string,@Body()b:unknown){await this.ok(r);return PlatformTenantSummarySchema.parse(await this.s.setTenantLifecycle(t,PlatformTenantLifecycleMutationSchema.parse(b),r.auth!.user.id));}@Get("/router-health")async router(@Req()r:AuthenticatedRequest){await this.ok(r);return RouterHealthSummarySchema.parse(await this.s.router());}@Get("/billing")async billing(@Req()r:AuthenticatedRequest){await this.ok(r);return BillingOperationsSummarySchema.parse(await this.s.billing());}@Get("/rollouts")async rollouts(@Req()r:AuthenticatedRequest){await this.ok(r);return z.array(RolloutRuleSchema).parse(await this.s.rollouts());}@Post("/rollouts")async rollout(@Req()r:AuthenticatedRequest,@Body()b:unknown){await this.ok(r);return this.s.upsertRollout(PlatformRolloutMutationSchema.parse(b),r.auth!.user.id);}}
